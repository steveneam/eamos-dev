# Worktree Inventory — RETIRED 2026-05-18

This file was a frozen 2026-05-17 worktree snapshot ("Branch: master /
nothing committed"). It went stale (the branch is now
`checkpoint/v2-batches-2026-05-17` with `9a27ef0`/`88a3739` committed) and
duplicated what git reports live.

**Use the live source instead:**

```
git status --short --branch
git diff --stat
```

The original snapshot is archived verbatim at
`agent_handoff/archive/2026-05-18-worktree-inventory.md` (README Hard Rule 1 —
nothing deleted). This file is no longer in the session read order; see
`agent_handoff/README.md` → "Read Order".
