# Archived: Claude `CURRENT.md` section — pre M3 integration + M7 scaffold

Archived 2026-05-28 00:06 +1000 by Claude before replacing the `## Active Status` Claude line. Preserves detail per README Hard Rule 1 (append + archive, never delete). The two `Claude` lines below are the verbatim state as of 2026-05-27 23:35 + the prior-prior pinned line.

---

- **Claude:** IDLE @ 2026-05-27 23:35 +1000 — **Wave 1 M-001 full ship + M-002 TS-mirror + planner-skill canonical restore.** Branch `checkpoint/v2-batches-2026-05-17`; origin at `3e484c6` (Codex pushed up to my batch-1); local 4 ahead of origin. **4 Claude commits + 1 Codex BE commit + 3 Codex handoff commits this session.** (1) **`6773fc3` planner-skill restore** — 13 quality_reviewer/ files relocated from gitignored `_bad/` to canonical path (BOM stripped); `orchestrator/executor.py` dash→underscore fix for 3 dispatch sites; `_bad/` and stale `.git/info/exclude` line deleted; all 5 QR workflows + exec_reconcile end-to-end. Codex PASS-verified at 22:44 (CAR closed). (2) **`d0f4eae` M-002 TS-mirror** — 8 new types + 2 thin helpers in `app/web/lib/{backend.ts,api.ts}` consuming Codex's M11 contract sketch (LookupSectionId / Status / SummaryTile / SectionDescriptor / InitialSummaryResponse / SectionFetchRequest / SectionFreshness / SectionEnvelope / SectionFetchResponse + `lookupSummary()` + `fetchLookupSections()`); payload typed as `Record<string, unknown> | null`, narrowed at read site; tsc clean; **Wave 3 unblocked**. (3) **`3e484c6` M-001 Wave 1 batch 1** — 3 additive primitives: AcmgCriteriaFold met/not-met → frozen-ramp tokens (globals.css 599-602), ClassificationBadge `reviewStars?` slot (4 inline-SVG stars + tabular numeral), Card `verdict?` prop (3px `--cls-*-dot` left border). Built by 3 parallel developer sub-agents. (4) **`472a7c3` M-001 Wave 1 batch 2** — 5 more components: CiteChip + CiteModal (§10.7 modal, `?cite=1` URL, focus-trap), StackedCountBar (proportional ramp bar), StickyVariantRibbon (pinned ribbon at top:60 past scrollY>200, prefers-reduced-motion compliant), PublicationModal (`?pub=PMID:N` URL with `usePublicationUrl` hook preserving other params), PublicationsCallout variant↔gene toggle (`?pubScope=` inbound URL). Built by 5 parallel developer sub-agents (3 high-difficulty modal/scroll components in parallel). **All 8 M-001 Tier-1 component upgrades shipped — Wave 1 COMPLETE.** None mounted yet; integration slice connects them later. **Commit-boundary incident** (recovered): Codex's parallel session twice used `git add -A`-equivalent staging, sweeping Claude FE files into backend-titled commits (628aeb5 then 3b2f13e). Steven approved a split → I produced clean `9a3d3a6` (BE-only, 26 files) + `472a7c3` (FE-only, 6 files, 1278 ins). 3b2f13e reset off branch by Codex; Codex now codified the explicit-paths-only rule in `agent_handoff/DECISIONS.md` (DL-019 reinforced). **Next session priority queue**: (a) M3 integration slice — mount CiteChip globally, wire StickyVariantRibbon + PublicationModal + StackedCountBar + the verdict prop + the stars slot at their report call sites (use the §10.9 plan's existing §6 backlog tracking, not new milestones); (b) M7 FE-only matrix overture (Wave 3 starter, no CAR needed — MatrixOverture + MatrixTile + page mount + URL fragments + mobile h-scroll); (c) per-slice CARs #2/#3/#4 open when M8/M9/M10a FE slices begin (never batched up front, DL-002); (d) M5 Workbench Phase 2 stays a decoupled lane. **Standing rule**: Codex handoff message appended after non-trivial shared-infra work ([[feedback_codex_handoff_message]]) — done for the planner restore + the commit-boundary incident. AlphaMissense stays hidden; trigger-watch unchanged ([[project_alphamissense_plan]]). AskEamos COMING SOON ([[feedback_askeamos_parked]]).

- **Claude (prior):** IDLE @ 2026-05-27 21:50 +1000 — **2 Claude/app/web commits shipped + LIVE on eamos-dev.vercel.app.** local==origin at `fe9e3b4` then `1a01059` handoff. (1) `fdfa9c9` rich-HTML copy payload — new `lib/report-html.ts` mirrors `report-tsv.ts` function-for-function (7 `htmlX()` serializers w/ deep-teal banner, italic variant line, sub-section bands, bordered/zebra/wrap cells, sized cols via `<colgroup>`, live hyperlinks); `CopyButton` accepts `{html, text}` and writes both via `ClipboardItem` — Excel picks up rich HTML, plain targets still get TSV. (2) `fe9e3b4` Workbench pass-2 slice 2 — Primer + CRISPR + Align panels ported verbatim (8 components w/ `'use client'`, 9 lib files, 3 mock-first api functions); `WorkbenchShell.renderToolPanel()` switch replaces 4 stubs (Compare keeps COMING SOON); `import.meta.env.VITE_*` → `process.env.NEXT_PUBLIC_*`. tsc clean both; Chrome MCP browser-verified. **Op gotcha:** clipboard probe via evaluate_script — always restore `navigator.clipboard.write`/`writeText` or reload after (leaked hijack causes silent copy failures; post-`fe9e3b4` console.warn surfaces this).
