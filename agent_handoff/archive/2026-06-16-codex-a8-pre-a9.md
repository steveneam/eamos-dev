# Codex A8 CURRENT archive before A9 replacement

Archived 2026-06-16 18:44 +1000 by Codex before replacing the Codex
`agent_handoff/CURRENT.md` section with the Epic A A9 closeout.

## Codex - Last Task & Resume

Owner-written by **Codex only**. Claude: read, never rewrite (README Rule 1/2).
Section last edited: 2026-06-16 04:25 +1000 - Codex.

**Latest Codex update (2026-06-16 04:25 +1000 - Codex):**
Epic A A8 is complete locally and verified. A9+ was intentionally not started; Steven approval is required before continuing Epic A. Prior A7 section archived at `agent_handoff/archive/2026-06-16-codex-a7-pre-a8.md`.

Completed:
- `LiteratureEmbeddingStore.query()` now caps gene-filtered candidate rows, streams the SQLite cursor instead of `fetchall()`, and uses an optional numpy fast path when numpy is installed, with the stdlib `array("f")` scorer retained as fallback. Follow-up: consider making numpy a normal Windows dependency in `app/backend/requirements.txt` if the install path is acceptable.
- Indexed source readers now fail closed before materializing unbounded results: VCF tabix range queries enforce max window bp and max rows, predictor-position tabix reads enforce max rows, and bigWig conservation summaries enforce max window bp while computing the mean in one pass.
- ClinGen local candidate search now uses normalized indexed terms instead of `lower(raw_json) LIKE`; materialization adds cDNA/protein search tokens extracted from source strings.
- Variant-library reads are capped/paginated, and SQLite/Supabase `save_variants()` now use batched upserts instead of per-variant DB transactions or per-variant HTTP POSTs.
- Chat/report inbound payloads now reject oversized client-supplied arrays/text before lookup-chat context slicing.
- Existing A8 guards confirmed already present: bulk save request `max_length=100` and CRISPR screening-primer sites `max_length=50`.

Verification:
- `python -m compileall app/backend/app/services/ai_gateway/retrieval.py app/backend/app/schemas/chat.py app/backend/app/schemas/run.py app/backend/app/services/indexed_sources.py app/backend/app/services/clingen_local.py app/backend/app/repos/variant_library_repo.py app/backend/app/services/variant_library.py app/backend/app/api/routes/variant_library.py`
- `python -m pytest app/backend/tests/test_literature_retrieval.py app/backend/tests/test_indexed_source_readers.py app/backend/tests/test_variant_library_api.py app/backend/tests/test_variant_library_supabase.py app/backend/tests/test_chat_service.py app/backend/tests/test_clingen_local.py -q`
- `python -m pytest app/backend/tests/test_frontend_contract.py app/backend/tests/test_run_chat_api.py -q`
- `python -m ruff check ...` on touched A8 service/schema/route/test files.
- `python -m black --check --fast ...` on touched A8 service/schema/route/test files.
- `git diff --check -- ...` on touched A8 tracked files passed apart from existing LF/CRLF warnings.
- `python -m graphify update .` first timed out at 120s, then passed with a 300s timeout; graphify reported no code-graph topology changes and left outputs untouched.
- No push/deploy/live Render verification this session.

**Latest resume prompt:**
```text
# Resume prompt - 2026-06-16 04:25 +1000 - Codex Epic A A8 complete
Eamos. Read AGENTS.md, agent_handoff/README.md, agent_handoff/CURRENT.md, agent_handoff/RISKS.md, docs/stability-audit/findings.md, then run git status --short --branch.
Delta: Epic A A8 completed locally and verified; no push/deploy. Bounded result-set/payload hardening now covers RAG literature retrieval, indexed source readers, ClinGen local candidate search, variant-library list/bulk paths, and chat/report inbound arrays. Optional numpy fast path added with stdlib fallback; consider normalizing numpy Windows dependency later.
Next: do not start A9+ unless Steven approves. If approved, continue Epic A from `docs/stability-audit/findings.md`. Keep held files excluded: `docs/proprietary/eamos-ai-gateway.md`, `scripts/eamos-encoding-scan.mjs`, `graphify-out/2026-06-15/`.
Guardrails: `LLM_PROVIDER=mock`; no Supabase apply/provider flip; explicit pathspecs, never git add -A; coordinate via Log Edit-Lock + Shared File Locks. End clear-safe.
```
