## Codex - Last Task & Resume

Owner-written by **Codex only**. Claude: read, never rewrite (README Rule 1/2).
Section last edited: 2026-06-16 01:34 +1000 - Codex.

**Latest Codex update (2026-06-16 01:34 +1000 - Codex):**
Epic A A2 + A1 are complete locally and verified. A3+ was intentionally not started after Steven requested one-A-per-session boundaries.

Completed:
- A2 hardened `POST /api/v1/batch/uploads`: auth, per-user rate limit, pre-form `Content-Length` rejection, bounded chunk read, streaming gzip/text decode with decompressed-byte ceiling, and a shared 5,000 parsed-variant cap.
- A1 made public `/protein/annotate`, source-backed `/viewer`, and lookup gene-context protein hydration cache-only/fail-closed so web requests no longer launch `hmmscan` on cache miss.
- A1 kept an explicit offline warmer path (`ProteinAnnotationService.warm_cache`) and guarded any allowed HMMER run with a process-wide semaphore, POSIX `RLIMIT_AS`, and safe default residue cap when `protein_annotation_hmmscan_max_residues<=0`.
- Ran `python -m graphify update .` (AST-only). It updated `graphify-out/graph.json`, `GRAPH_REPORT.md`, and `manifest.json`, created `graphify-out/2026-06-16/`, and skipped `graph.html` because the graph is above the 5,000-node HTML threshold.

Verification:
- `python -m pytest app/backend/tests/test_vcf_ingest.py app/backend/tests/test_batch_api.py app/backend/tests/test_rate_limits.py app/backend/tests/test_protein_annotation_service.py app/backend/tests/test_gene_viewer.py -q`
- `python -m pytest app/backend/tests/test_frontend_contract.py -q`
- `python -m compileall` on touched backend modules.
- `git diff --check` on touched backend files passed apart from existing LF/CRLF warnings.
- No push/deploy/live Render verification this session.

**Latest resume prompt:**
```text
# Resume prompt - 2026-06-16 01:34 +1000 - Codex Epic A A3 next
Eamos. Read AGENTS.md, agent_handoff/README.md, agent_handoff/CURRENT.md, agent_handoff/RISKS.md, docs/stability-audit/findings.md, then run git status --short --branch.
Delta: Epic A A2 + A1 completed locally and verified; no push/deploy. A2 hardened batch VCF upload. A1 made public protein annotation/viewer/lookup protein hydration cache-only, added HMMER semaphore/RLIMIT guard, and made hmmscan cap<=0 use safe 5000. `python -m graphify update .` ran; it updated graphify-out and skipped graph.html due >5000 nodes.
Next: start Epic A A3 only: cache `gene_context_snapshot` including `protein_domain_track` in variant_cache_repo and stop table-wide DELETE-on-read in `variant_cache_repo.py`. Do not start A4+ unless Steven approves. Keep held files excluded: `docs/proprietary/eamos-ai-gateway.md`, `scripts/eamos-encoding-scan.mjs`, `graphify-out/2026-06-15/`.
Guardrails: `LLM_PROVIDER=mock`; no Supabase apply/provider flip; explicit pathspecs, never git add -A; coordinate via Log Edit-Lock + Shared File Locks. End clear-safe.
```
