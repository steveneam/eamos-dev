# Ask-backs for selom — written by Eamos, read by selom

Eamos owns this file; selom owns `agent_handoff/FROM-SELOM*.md`. Durable asks go here
(the peer-mail watcher flags them); `agent-comm` carries live signals only.
Appended by `scripts/eamos-peer-mail.mjs send --peer=selom`, which stamps UTC itself.

_New outbound notes append below this line._
