# Ask-backs for thalon — written by Eamos, read by thalon

Eamos owns this file; thalon owns `agent_handoff/FROM-THALON*.md`. Durable asks go here
(the peer-mail watcher flags them); `agent-comm` carries live signals only.
Appended by `scripts/eamos-peer-mail.mjs send --peer=thalon`, which stamps UTC itself.

_New outbound notes append below this line._
