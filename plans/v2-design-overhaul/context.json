{
  "task_spec": [
    "subject: Design-first frontend overhaul of 3 Eamos surfaces to a 'scientific accuracy meets quiet luxury' feel, driven by 4 ingested design-philosophy docs; produce a reviewable step-by-step task plan PER PAGE (landing, /report variant evidence report, /workbench)",
    "scope: app/frontend/** only (components, pages, lib, src/index.css, src/styles/workbench.css); DESIGN.md token additions",
    "out-of-scope: app/backend/**, backend.ts contract SHAPE changes (backend-led; Codex syncs real data later), /runs LegacyRunsApp (frozen + on hold), AlphaMissense re-enable (on hold), real PDF/Excel export generation + real VCF parsing + real ancestry data (visual shells with sample data only)"
  ],
  "constraints": [
    "MUST: frontend-only this pass; no backend.ts contract reshape (Hard Rule 5, backend-led); request via Cross-Agent if needed",
    "MUST: do not touch /runs or AlphaMissense (both user-mandated ON HOLD; DECISIONS.md)",
    "MUST: evolve existing DESIGN.md v2 tokens (extend :root scale, add navy-header tier) - not a full rewrite",
    "MUST: navy = header/chrome ONLY, NOT page background; white/off-white content surfaces; teal buttons/accents; neutral grays + subtle warm neutral (user-clarified)",
    "MUST: preserve clinical core - 0.5px hairlines, muted palette, semantic classification colours, no font weight >700, motion only via --dur/--ease tokens, ship prefers-reduced-motion guard",
    "MUST: net-new surfaces (multi-variant/VCF, ancestry heatmap, export menu) = working visual shells on sample/placeholder data",
    "MUST: plan reviewed/approved by user BEFORE implementation",
    "SHOULD: reference feel = modern AI-company landing pages (Claude/OpenAI/Gemini/Grok) + high-end medical device / premium analytics dashboard",
    "SHOULD: keep vitest 73/73 green, build clean, contract canary 40/40 (no backend.ts shape change); browser-verify Workbench UI changes",
    "SHOULD: per-page plans independently reviewable/deliverable; shared scroll-transition search built once and reused"
  ],
  "entry_points": [
    "src/pages/LandingPage.tsx - split hero(text)+PreviewCard; SourceStrip/FeaturesGrid/HowItWorks/SiteFooter; no centered hero, no scroll-transition",
    "src/pages/ReportPage.tsx - TopNav nav-SearchShell; 920px col; numbered Cards 2-8 vertical; ReportBody composition; no key-metrics bar, no section teaser/expand, no section nav, no export menu",
    "src/pages/WorkbenchPage.tsx - bespoke .nav-wrap nav (NOT shared TopNav); ContextStrip; WorkbenchShell 3-col 64px/1fr/360px",
    "src/components/search/SearchShell.tsx - single comp, hero|nav variants, static sample chips, NO scroll-transition/sticky, NO autocomplete dropdown",
    "src/components/layout/TopNav.tsx + ModePill.tsx - shared report chrome (workbench reimplements its own)",
    "src/index.css - :root design tokens (light clinical system); src/styles/workbench.css - workbench .nav/.ns-shell classes",
    "src/lib/sample-report.ts (RPE65_SAMPLE), src/lib/sources.ts, src/lib/workbench/*-sample.ts - sample data for shells",
    "src/components/report/* - VariantHeader, AIStack, LocusContext, InSilicoGrid, EvidenceTable, AcmgCriteriaFold, CuratedVariantsGrid, AssociatedConditions, PublicationsCallout, etc.",
    "src/components/workbench/* - WorkbenchShell, ToolRail/ToolBar, SidePanel, CanvasHeader, viewer/* (SequenceViewerV2, GeneMinimap, ProteinView, EditPopoverV2), primer/*, crispr/*"
  ],
  "rejected_alternatives": [
    "Full navy-dominant re-theme (DESIGN.md v3, navy bg all pages) - rejected: risks dense evidence-table legibility; user chose navy=header-only",
    "Structure-only, palette unchanged - rejected: user wants the quiet-luxury mood shift, not just new behaviors",
    "Reserve-space + stub net-new features - rejected: user wants reviewable visual shells with sample data",
    "Defer net-new entirely - rejected: same",
    "Start redesign on dirty worktree / commit FE+BE together - rejected: user chose FE-lane-only checkpoint (done: 205eaae)"
  ],
  "current_understanding": [
    "Mature DESIGN.md v2: light clinical, white surfaces, navy as INK only, single teal accent, 0.5px hairlines, 'dense but scannable', 3-layer progressive-disclosure pattern, elevation/motion tokens, Syne/Plus Jakarta Sans/JetBrains Mono",
    "Philosophy in the 4 docs is ~80% already encoded in DESIGN.md (Rams, progressive disclosure, restraint, teal, calm/trust, whitespace, motion). Real delta is structural + a navy-header mood tier",
    "Landing today: split text+PreviewCard, static, no scroll behavior",
    "Report today: numbered Cards vertical, VariantHeader+AIStack then Cards 2-8; no above-fold key-metrics bar; only AcmgCriteriaFold uses disclosure; no in-page section nav; export is just a VariantHeader 'Export PDF' tool link",
    "Workbench today: closest to doc; bespoke nav not shared TopNav; WorkbenchShell rail/canvas/sidepanel; GeneMinimap exists but no top overview-ruler/minimap-as-orientation; side panel fixed 360px not resizable; <1200px collapses panel, <760px collapses rail (display:none)",
    "SearchShell: hero|nav variants; no scroll-transition continuity; sample chips only (no autocomplete dropdown)",
    "FE-lane checkpoint committed 205eaae; backend uncommitted by design; vitest 73/73, build clean"
  ],
  "assumptions": [
    "Doc 'Gene/Sequence Viewer Page' == existing /workbench surface (H)",
    "Sample data sufficient for all shells: RPE65_SAMPLE + workbench *-sample.ts cover report+workbench; landing needs only static/illustrative (H)",
    "Multi-variant/VCF + export + ancestry heatmap can be sample-driven without backend.ts shape change (M - may need additive FE-only sample types, not contract)",
    "Workbench should adopt the shared TopNav navy-header tier for cross-surface continuity (M - currently bespoke nav)",
    "Scroll-transition search is one shared component reused by landing+report+workbench headers (H)"
  ],
  "invisible_knowledge": [
    "Naming: NEVER bare 'report'. 'Variant Evidence Report' = /report = ReportPage = active focus. 'Patient Report Pipeline' = /runs = on hold, do not touch",
    "Contract changes are backend-led; test_frontend_contract.py is the canary (currently 40/40); FE mirrors, never reshapes backend.ts",
    "DESIGN.md invariants: no ad-hoc box-shadow (only --elev-*), no decorative gradients (<=4% functional wash only), no weight>700, no radius>14px card/100px pill, no pure black (#0b1a2b ink), no Franklin chip/link, semantic classification colours never change",
    "Existing focus glow 0 0 0 3px rgba(29,158,117,.12) is the generalised affordance ring; reuse it",
    "Workbench dev needs browser/pixel verification after meaningful UI change (RISKS.md)",
    "CSS comments must not contain */ (Tailwind v4 Lightning CSS strict); build ~50s, chunk+plugin-timings warnings are NOT failures",
    "Coordination: agent_handoff/ protocol; update CURRENT.md Active Status heartbeat + release Log Edit-Lock each session; narrative only at major boundaries (replace-never-stack); end clear-safe with stamped resume prompt",
    "Tradeoff accepted: visual shells now, real data via Codex later, to reduce repetitive FE<->BE back-and-forth (user explicit)"
  ],
  "reference_docs": [
    "c:/Users/seamegdool/Desktop/Claude code and website tips/EAMOS Web Tool/Design/Core Philosophy & Design Theory - Landing page.txt - landing 'Input as Hero', scroll-transition, palette",
    "...Report page 1.txt - 'Content as Hero', variant hero summary, modular cards, section nav",
    "...Report page 2.txt - progressive disclosure teaser/expand, key-metrics bar, multi-variant/VCF, export needs",
    "...Workbench.txt - viewer-as-hero split-pane, tracks, side panels, sync, data-source stamps",
    "E:/eamos/DESIGN.md - design system v2 tokens/components/invariants (evolve, don't rewrite)",
    "E:/eamos/agent_handoff/README.md - coordination protocol; CURRENT.md/RISKS.md/DECISIONS.md - locks, gated work, holds",
    "E:/eamos/CLAUDE.md - golden rule (Claude=FE), coding guidelines",
    "C:/Users/seamegdool/.claude/plans/next-session-eamos.md - Claude rolling handoff log"
  ]
}
