[project]
name = "hsil-demo-backend"
version = "0.1.0"
description = "Fixture-first backend for the HSIL 2026 demo"
requires-python = ">=3.10"

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.ruff]
line-length = 100

[tool.black]
line-length = 100
