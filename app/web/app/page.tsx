import { LandingClient } from '@/components/landing/LandingClient'

// Landing route (`/`). Server component → renders the ported client landing.
export default function Page() {
  return <LandingClient />
}
